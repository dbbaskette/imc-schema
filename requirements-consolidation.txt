# Requirements for Parquet Consolidation Utility
# Install with: pip install -r requirements-consolidation.txt

# Core data processing
pandas>=1.5.0
pyarrow>=10.0.0

# HDFS connectivity
hdfs3>=0.3.1

# Alternative HDFS libraries (choose one)
# pydoop>=2.0.0        # Alternative HDFS client
# snakebite-py3>=3.0.5 # Pure Python HDFS client

# Optional: Enhanced logging and progress
tqdm>=4.64.0          # Progress bars
colorlog>=6.0.0       # Colored logging

# Optional: Configuration management  
pyyaml>=6.0           # YAML config files
click>=8.0.0          # Enhanced CLI framework
